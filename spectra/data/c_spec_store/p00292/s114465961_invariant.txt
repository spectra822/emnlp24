```c
int main()
    // Input format: The first line contains an integer `n`.
    // The next `n` lines each contain two integers `x` and `y`, separated by whitespace.
    // Output format: `n` lines, each containing a single integer.
    // Precondition: The global variables `n`, `x`, and `y` have been declared.
    // Postcondition: For each pair of integers `x` and `y`, the output is `(x-1)%y + 1` on a new line.
```