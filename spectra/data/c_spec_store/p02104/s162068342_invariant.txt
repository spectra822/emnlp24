```c
int in()
    // Precondition: None.
    // Postcondition: Reads a sequence of digits from the standard input until a non-digit character is encountered and returns the integer value of the digits read.

int main()
    // Input format: The first line contains an integer `n`.
    // The next `n` lines each contain an integer.
    // Output format: `n` lines, each containing an integer.
    // Precondition: The global arrays `p`, `sp`, `q`, `hi`, and `ans` have been declared.
    // Postcondition: For each integer in the input, prints the same integer on a new line.
```