```c
int main(void)
    // Input format: The first line contains an integer `n`.
    // The next `n` lines contain an integer.
    // Output format: A single number.
    // Precondition: The variable `n` has been declared.
    // Postcondition: The variable `count` is printed, representing the number of times a sequence of three consecutive numbers is either strictly increasing or strictly decreasing.
```